// src/types/overview.ts
export interface DataEntry {
  name: string;
  value: number;
}

// In a new file (e.g., src/types/overview.ts) or at the top of your file
export interface TopProduct {
  category: string;
  product: string;
  amount: number;
}
